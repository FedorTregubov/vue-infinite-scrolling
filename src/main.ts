import { createApp } from 'vue'
import App from './App.vue'

// import VueInfiniteScrolling from './index';
// import './components/vue-infinite-scrolling.css';

createApp(App)
  // .use(VueInfiniteScrolling)
  .mount('#app')
